import 'package:flutter/material.dart';

abstract class AppColor {
  // --- Brand Colors ---
  static const brandGreen = Color(0xFF52AE31);

  // --- Light Mode ---
  static const backgroundLight = Color(0xFFECF1F5);
  static const navBarBackgroundLight = Color(0xFFFFFFFF);
  static const navBarInactiveItemLight = Color(0xFF717171);

  // --- Dark Mode ---
  static const backgroundDark = Color(0xFF2A2B2F);
  static const navBarBackgroundDark = Color(0xFF37383C);
  static const navBarInactiveItemDark = Color(0xFFBFC3C8);

  // --- Neutral ---
  static const white = Colors.white;
  static const black = Colors.black;
  static const red = Colors.red;
  static const orange = Colors.orange;
}
